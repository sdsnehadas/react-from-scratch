import { render , screen, jestdom} from "@testing-library/react";
import RestaurentCard from '../components/RestaurentCard'
import MOCK_DATA from './mocks/restaurentCard.mock.json'

it("should render restaurentCard with data", ()=>{
   expect(1+1).toBe(2)
})